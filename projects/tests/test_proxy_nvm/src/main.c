#include <stdio.h>

int run()
{
// This is how the basic test will look like
//    ProxyNVM nvm;
//
//    bool ok = ProxyNVM_ctor(&nvm, ...);
//    // write something, and print it out
//    NVM_write(ProxyNVM_TO_NVM(&nvm), ...)
//    Debug_LOG_INFO(...)
//    // read it back and print out what was read
//    NVM_read(ProxyNVM_TO_NVM(&nvm), ...)
//    Debug_LOG_INFO(...)
    return 0;
}
