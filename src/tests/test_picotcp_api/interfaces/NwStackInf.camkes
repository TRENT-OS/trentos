/* Apps Interface for NW Stack api */
procedure NwStackInf {
    int     socket       (  in int domain, in int type);
    int     close        ( void );
    int     write        ( in int len );
    int     read         ( in int len );
    int     connect      ( in string serverName, in int port );
    int    accept       ( in uint16_t port );
    int    listen       ( in int backlog );
    int    bind         ( in uint16_t port );
   
};