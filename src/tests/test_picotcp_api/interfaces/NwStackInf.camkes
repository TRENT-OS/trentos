/*
 *  SEOS Nw Stack Interface
 *
 *  Copyright (C) 2019, Hensoldt Cyber GmbH
 *
 */
procedure NwStackInf {
 
    include <seos_err.h>;
    
 seos_err_t    socket_create ( in int domain, in int type, inout int pHandle );
 seos_err_t    socket_close  ( in int handle );
 seos_err_t    socket_write  ( in int handle,inout size_t pLen );
 seos_err_t    socket_read   ( in int handle,inout size_t pLen );
 seos_err_t    socket_connect( in int handle,in string serverName, in int port );
 seos_err_t    socket_accept ( in int handle,inout int pClient, in uint16_t port );
 seos_err_t    socket_listen ( in int handle,in int backlog );
 seos_err_t    socket_bind   ( in int handle,in uint16_t port );
 void          network_init  ( void);
   
};