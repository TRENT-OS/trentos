import "../../interfaces/ChanMuxInf.camkes";
import "../../interfaces/UartInf.camkes";

component ChanMux {

    emits     DataAvailableEvent  dataAvailableMain;
    provides  ChanMuxInInf        ChanMuxIn;
    //-------------------------------------------------
    // interface to ProxyNVM_Tester1
    dataport  Buf                 proxyNVM_Tester1DataPort;
    //-------------------------------------------------
    // interface to ProxyNVM_Tester2
    dataport  Buf                 proxyNVM_Tester2DataPort;
    
    //-------------------------------------------------
    // MUX Uart interface
    dataport  Buf                 outputDataPort;
    uses      UartInf             Output;
    provides  ChanMuxOutInf       ChanMuxOut;

    //-------------------------------------------------
    // Synchronization Primitives
    has mutex Mutex;
}
